# 🎰 日本ロット 番号推薦 (Japan Lottery Recommender)

[한국어](README.md) | [English](README.en.md) | [日本語](README.ja.md)

日本のロト(ロト6 / ロト7 / ミニロト)の番号推薦および当選番号照会ができるWebアプリケーションです。

**Demo → [Vercel デプロイURL]**

---

## 主な機能

### 番号推薦
| 機能 | 説明 |
|------|------|
| 🎲 **ランダム** | 完全ランダム抽出 |
| 📊 **頻度ベース** | 過去の当選履歴から出現頻度の高い番号に重み付け |
| ☯️ **五行ベース** | 当日の旧暦の干支(天干)から五行(木火土金水)を判定し、該当番号グループを優先選択 |
| 🔀 **複数方式の組み合わせ** | 複数の方式を同時選択した場合、各方式の重みを統合して最適な番号を算出 |
| 📌 **固定番号** | 最大3個の番号を直接指定して必ず含める |
| 🔒 **過去の当選組み合わせ除外** | すでに当選した組み合わせは自動的に除外して再生成 |
| 🍀 **ラッキーナンバー** | 40%の確率で直近3回の当選番号のうち1個を自動的に含める |
| 💬 **推薦理由の表示** | 各セットごとに番号選択の根拠を詳しく表示 |

### 当選番号照会
- ロト6 / ロト7 / ミニロトの最新当選番号をリアルタイムで照会
- ページネーションで過去の回号を閲覧可能
- データソース: [lottolyzer.com](https://lottolyzer.com)

---

## 技術スタック

| 分類 | 技術 |
|------|------|
| バックエンド | Python 3.9+, Flask |
| フロントエンド | Vanilla JS, HTML5, CSS3 |
| スクレイピング | BeautifulSoup4, urllib |
| デプロイ | Vercel (サーバーレス Python) |

---

## システムアーキテクチャ

```
ブラウザ
  │
  ├── GET /               → index.html を描画
  ├── POST /api/generate  → 番号推薦 (recommender.py)
  └── GET /api/results    → 当選番号照会 (data.py → lottolyzer.com をスクレイピング)

recommender.py
  ├── random モード: random.sample()
  ├── frequency モード: 出現頻度に基づく重み付きサンプリング
  ├── saju モード: 干支 → 五行グループを優先選択
  └── 複数モード: 各重みを掛け合わせ(積)て統合サンプリング

data.py
  ├── get_results()  : ページ単位のリアルタイムスクレイピング
  └── get_history()  : 推薦エンジン用の履歴(メモリキャッシュ)
```

---

## ファイル構成

```
lotto/
├── api/
│   └── index.py          # Vercel サーバーレスのエントリーポイント
├── static/
│   ├── app.js            # フロントエンドロジック
│   └── style.css         # スタイル
├── templates/
│   └── index.html        # シングルページUI
├── app.py                # Flask アプリ & ルーティング
├── data.py               # スクレイピング & データ管理
├── recommender.py        # 番号推薦エンジン
├── vercel.json           # Vercel デプロイ設定
├── requirements.txt
└── README.md
```

---

## API 仕様

### `POST /api/generate/<ltype>/<count>`
番号推薦を生成

| パラメータ | 位置 | 説明 |
|----------|------|------|
| `ltype` | path | `loto6` / `loto7` / `miniloto` |
| `count` | path | 生成するセット数 (1〜10) |
| `modes` | body | 推薦方式の配列 `["random","frequency","saju"]` |
| `fixed` | body | 固定番号の配列 (最大3個) |

**レスポンス例**
```json
{
  "results": [
    {
      "numbers": [2, 14, 21, 26, 33, 40],
      "bonus": [18],
      "mode": "frequency+saju",
      "reason": [
        "高頻度寄与: 26番(2.5%), 33番(2.4%)",
        "五行寄与 — 本日の干支: 庚日 → 金(金)",
        "五行該当番号: [26, 33]"
      ]
    }
  ]
}
```

### `GET /api/results/<ltype>?page=1&per_page=50`
当選番号照会

| パラメータ | 説明 |
|----------|------|
| `page` | ページ番号 (デフォルト 1) |
| `per_page` | 1ページあたりの結果数 (デフォルト 50, 最大 100) |

---

## 五行ロジック詳細

| 天干 | 五行 | ロト6 該当番号 |
|------|------|-----------------|
| 甲 乙 | 木 | 1 〜 8 |
| 丙 丁 | 火 | 9 〜 17 |
| 戊 己 | 土 | 18 〜 26 |
| 庚 辛 | 金 | 27 〜 34 |
| 壬 癸 | 水 | 35 〜 43 |

> 天干計算基準: 1984-02-02(甲子日)からの経過日数 mod 10

---

## ローカル実行

```bash
# 1. 依存関係のインストール
pip install -r requirements.txt

# 2. サーバー起動
python app.py

# 3. ブラウザでアクセス
http://localhost:5001
```

---

## Vercel デプロイ

```bash
# 1. Vercel CLI のインストール
npm i -g vercel

# 2. プロジェクトルートでデプロイ
cd lotto
vercel

# 3. 以降のアップデート
vercel --prod
```

> **注意:** Vercel 無料プランの関数タイムアウトは10秒です。
> 当選番号照会は lottolyzer.com からリアルタイムでスクレイピングするため、初回ロード時に1〜3秒かかります。

---

## ロトの種類別ルール

| 名前 | 番号範囲 | 本数字 | ボーナス | 抽選日 |
|------|-----------|--------|--------|--------|
| ロト6 | 1 〜 43 | 6個 | 1個 | 月・木 |
| ロト7 | 1 〜 37 | 7個 | 2個 | 金 |
| ミニロト | 1 〜 31 | 5個 | 1個 | 火 |

---

## 免責事項

このアプリケーションは**娯楽目的**で制作されています。
番号推薦アルゴリズムは当選を保証するものではなく、宝くじの購入はご自身の判断と責任において行ってください。

---

*Built with Flask & Vercel*
