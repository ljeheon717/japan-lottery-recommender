# 🎰 日本ロット 番号推薦 (Japan Lottery Recommender)

일본 로또(ロト6 / ロト7 / ミニロト) 번호 추천 및 당첨 번호 조회 웹 애플리케이션입니다.

**Demo → [Vercel 배포 URL]**

---

## 주요 기능

### 번호 추천

| 기능                       | 설명                                                                       |
| -------------------------- | -------------------------------------------------------------------------- |
| 🎲 **랜덤**                | 순수 무작위 추출                                                           |
| 📊 **빈도 기반**           | 전체 당첨 이력에서 출현 빈도가 높은 번호에 가중치 부여                     |
| ☯️ **오행 기반**           | 당일 음력 일진(천간)으로 오행(木火土金水)을 판단, 해당 번호 그룹 우선 선택 |
| 🔀 **복수 방식 조합**      | 여러 방식 동시 선택 시 각 방식의 가중치를 통합하여 최적 번호 산출          |
| 📌 **고정 번호**           | 최대 3개 번호를 직접 지정하여 반드시 포함                                  |
| 🔒 **과거 당첨 조합 제외** | 이미 당첨된 적 있는 조합은 자동으로 제외 후 재생성                         |
| 🍀 **행운 번호**           | 40% 확률로 최근 3회차 당첨 번호 중 1개를 자동 포함                         |
| 💬 **추천 이유 출력**      | 각 세트마다 번호 선택 근거를 상세히 표시                                   |

### 당첨 번호 조회

- ロト6 / ロト7 / ミニロト 최근 당첨 번호 실시간 조회
- 페이지네이션으로 이전 회차 탐색 가능
- 데이터 출처: [lottolyzer.com](https://lottolyzer.com)

---

## 기술 스택

| 분류       | 기술                       |
| ---------- | -------------------------- |
| Backend    | Python 3.9+, Flask         |
| Frontend   | Vanilla JS, HTML5, CSS3    |
| Scraping   | BeautifulSoup4, urllib     |
| Deployment | Vercel (Serverless Python) |

---

## 시스템 아키텍처

```
브라우저
  │
  ├── GET /               → index.html 렌더링
  ├── POST /api/generate  → 번호 추천 (recommender.py)
  └── GET /api/results    → 당첨 번호 조회 (data.py → lottolyzer.com 스크래핑)

recommender.py
  ├── random  모드: random.sample()
  ├── frequency 모드: 출현 빈도 기반 가중치 샘플링
  ├── saju 모드: 일진 → 오행 그룹 우선 선택
  └── 복수 모드: 각 가중치 곱산(product) → 통합 샘플링

data.py
  ├── get_results()  : 페이지 단위 실시간 스크래핑
  └── get_history()  : 추천 엔진용 이력 (메모리 캐시)
```

---

## 파일 구조

```
lotto/
├── api/
│   └── index.py          # Vercel 서버리스 진입점
├── static/
│   ├── app.js            # 프론트엔드 로직
│   └── style.css         # 스타일
├── templates/
│   └── index.html        # 단일 페이지 UI
├── app.py                # Flask 앱 & 라우팅
├── data.py               # 스크래핑 & 데이터 관리
├── recommender.py        # 번호 추천 엔진
├── vercel.json           # Vercel 배포 설정
├── requirements.txt
└── README.md
```

---

## API 명세

### `POST /api/generate/<ltype>/<count>`

번호 추천 생성

| 파라미터 | 위치 | 설명                                           |
| -------- | ---- | ---------------------------------------------- |
| `ltype`  | path | `loto6` / `loto7` / `miniloto`                 |
| `count`  | path | 생성 세트 수 (1~10)                            |
| `modes`  | body | 추천 방식 배열 `["random","frequency","saju"]` |
| `fixed`  | body | 고정 번호 배열 (최대 3개)                      |

**응답 예시**

```json
{
  "results": [
    {
      "numbers": [2, 14, 21, 26, 33, 40],
      "bonus": [18],
      "mode": "frequency+saju",
      "reason": [
        "고빈도 기여: 26번(2.5%), 33번(2.4%)",
        "오행 기여 — 오늘 일진: 경일 → 금(金)",
        "오행 해당 번호: [26, 33]"
      ]
    }
  ]
}
```

### `GET /api/results/<ltype>?page=1&per_page=50`

당첨 번호 조회

| 파라미터   | 설명                                   |
| ---------- | -------------------------------------- |
| `page`     | 페이지 번호 (기본값 1)                 |
| `per_page` | 페이지당 결과 수 (기본값 50, 최대 100) |

---

## 오행 로직 상세

| 천간          | 오행 | ロト6 해당 번호 |
| ------------- | ---- | --------------- |
| 갑(甲) 을(乙) | 木   | 1 ~ 8           |
| 병(丙) 정(丁) | 火   | 9 ~ 17          |
| 무(戊) 기(己) | 土   | 18 ~ 26         |
| 경(庚) 신(辛) | 金   | 27 ~ 34         |
| 임(壬) 계(癸) | 水   | 35 ~ 43         |

> 천간 계산 기준: 1984-02-02(갑자일)로부터 경과 일수 mod 10

---

## 로컬 실행

```bash
# 1. 의존성 설치
pip install -r requirements.txt

# 2. 서버 시작
python app.py

# 3. 브라우저에서 접속
http://localhost:5001
```

---

## Vercel 배포

```bash
# 1. Vercel CLI 설치
npm i -g vercel

# 2. 프로젝트 루트에서 배포
cd lotto
vercel

# 3. 이후 업데이트
vercel --prod
```

> **주의:** Vercel 무료 플랜 함수 타임아웃은 10초입니다.  
> 당첨 번호 조회는 lottolyzer.com에서 실시간 스크래핑하므로 첫 로드 시 1~3초 소요됩니다.

---

## 로또 종류 규칙

| 이름     | 번호 범위 | 본번호 | 보너스 | 추첨일 |
| -------- | --------- | ------ | ------ | ------ |
| ロト6    | 1 ~ 43    | 6개    | 1개    | 월·목  |
| ロト7    | 1 ~ 37    | 7개    | 2개    | 금     |
| ミニロト | 1 ~ 31    | 5개    | 1개    | 화     |

---

## 면책사항

이 애플리케이션은 **오락 목적**으로 제작되었습니다.  
번호 추천 알고리즘은 당첨을 보장하지 않으며, 복권 구매는 본인의 판단과 책임 하에 이루어져야 합니다.

---

_Built with Flask & Vercel_
